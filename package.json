{
  "name": "fruit-clicker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd android && chmod +x gradlew && ./gradlew assembleDebug || ./gradle/bin/gradle assembleDebug"
  }
}